# Product‑API

API для управления товарами, реализованное на Go.

## 📌 Описание

Проект представляет собой простое REST‑API на языке Go, которое позволяет:

- создавать, читать, обновлять и удалять (CRUD) товары;
- хранить данные о товарах (например: название, описание, цена, наличие);
- лёгкую и расширяемую архитектуру с разделением на модели, обработчики, хранилище.

Цель — служить учебным примером структуры Go‑приложения и основ работы с REST.

## 🛠 Технологии

- Go (Golang)
- Модульная система `go.mod`/`go.sum`
- Архитектура с папками: `models`, `handlers`, `store`
- Файл `main.go` — точка входа

## 🚀 Быстрый старт

```bash
# 1. Клонируйте репозиторий
git clone https://github.com/DovletEmin/Product-API.git
cd Product-API

# 2. Установите зависимости
go mod download

# 3. Запустите API
go run main.go
```

По умолчанию API будет доступен по адресу `http://localhost:8080`.

## 🧪 Примеры запросов

```bash
# Получить список товаров
curl http://localhost:8080/products

# Получить товар по ID
curl http://localhost:8080/products/{id}

# Создать новый товар
curl -X POST http://localhost:8080/products \
     -H "Content-Type: application/json" \
     -d '{"name":"Новый товар","description":"Описание","price":123.45,"in_stock":true}'

# Обновить товар
curl -X PUT http://localhost:8080/products/{id} \
     -H "Content-Type: application/json" \
     -d '{"name":"Обновлённый товар","description":"Новое описание","price":150.00,"in_stock":false}'

# Удалить товар
curl -X DELETE http://localhost:8080/products/{id}
```

## 📂 Структура проекта

```
├── main.go               # Точка входа
├── go.mod                # Модули Go
├── go.sum                # Контроль зависимостей
├── .gitignore            # Игнорируемые файлы
├── models/               # Определения моделей данных
├── handlers/             # HTTP‑обработчики
└── store/                # Работа с хранилищем
```

## 🔧 Конфигурация

- `PORT` — порт (по умолчанию 8080)
- `DATABASE_URL` — URL подключения к БД (если используется)
- `LOG_LEVEL` — уровень логирования

## ✅ Что уже сделано

- CRUD‑операции для товаров
- Чистая структура проекта

## 🛠 Что можно улучшить

- Аутентификация (JWT)
- Подключение PostgreSQL/MySQL
- Тестирование и CI/CD
- Контейнеризация с Docker

## ✍ Автор

**Dovlet Eminov (Emin)**  
📍 Ashgabat, Turkmenistan  
📧 dovlet.eminov26.02@gmail.com
